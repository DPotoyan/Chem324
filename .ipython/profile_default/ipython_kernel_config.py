# Retina (2x) PNGs for matplotlib outputs baked into the site: same displayed
# size, double the pixels, so plots stay crisp on hiDPI screens and projectors.
# Activated by IPYTHONDIR=$PWD/.ipython during `myst build --execute`.
c.InlineBackend.figure_format = "retina"
